<form method="post" action="{{ path('piecedetachee_delete', {'id': piecedetachee.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cette annonce ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piecedetachee.id) }}">
    <button class="btn btn-danger">Supprimer l'annonce</button>
</form>
