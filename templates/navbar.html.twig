<nav class=" navbar navbar-expand-lg bg-dark navbar-dark">
    <a class="navbar-brand"
       href="{{ path('lmdtr_index') }}">
        Accueil
    </a>
    <button class="navbar-toggler"
            type="button"
            data-toggle="collapse"
            data-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse"
         id="navbarNav">
        <ul class="navbar-nav">
            <li>
                <div class="container">
                    <div class="dropdown">
                        <button class="btn dropdown-toggle grey px-0" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Consulte les annonces
                        </button>
                        <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                            <a class="dropdown-item grey" href="{{ path('moto_index') }}">Moto</a>
                            <a class="dropdown-item grey" href="#">Piéces détachées</a>
                        </div>
                    </div>
                </div>
            </li>
            {% if  is_granted("ROLE_SUBSCRIBER") %}
                <li>
                    <div class="container">
                        <div class="dropdown">
                            <button class="btn dropdown-toggle grey px-0" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Depose une annonce
                            </button>
                            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                                <a class="dropdown-item grey" href="{{ path('moto_new') }}">Moto</a>
                                <a class="dropdown-item grey" href="#">Piéces détachées</a>
                            </div>
                        </div>
                    </div>
                </li>
                <li class="nav-item">
                    <a class="nav-link"
                       href="#">
                        Discute
                    </a>
                </li>
            {% endif %}
            <li class="nav-item">
                <a class="nav-link"
                   href="{{ path('contact') }}">
                    Contact nous
                </a>
            </li>
            <div class="row connection">
                {% if not app.user %}
                    <li class="nav-item">
                        <a href="{{ path('app_login') }}"
                           class="nav-link">
                            Connexion
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('security_registration') }}"
                           class="nav-link">
                            S'inscrire
                        </a>
                    </li>
                {% endif %}
                {% if app.user %}
                    <li class="nav-item">
                        <a href="{{ path('app_logout') }}"
                           class="nav-link">
                            Déconnexion
                        </a>
                    </li>
                    <li class="nav-item">
                        <p class="nav-link"> {{ app.user.username}}</p>
                    </li>
                {% endif %}
            </div>
        </ul>
    </div>
</nav>
