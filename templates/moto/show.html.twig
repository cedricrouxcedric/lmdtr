{% extends 'base.html.twig' %}

{% block title %}Moto{% endblock %}

{% block body %}
    <div class="jumbotron pt-3">
        <div class="container">
            <h1 class="text-center">Moto</h1>
            <div class="rown justify-content-center">
                <div class="col-12 text-center">
                    <div class="">
{#                    {% if moto.filename %}#}
{#                        <img src="{{ asset(moto,'imageFile') | imagine_filter('thumb') }}"#}
{#                    {% endif %}#}
                    </div>
                </div>
                <div class="col-md-8">
{#                    <h1 class="text-center text-danger">{{ moto.filename }}</h1>#}
                    <h2>{{ moto.marque.name }}</h2>
                    <p> model de la moto</p>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-8">
            <table class="table table-striped">
                <tr>
                    <td>Categorie</td>
                    <td>{{ moto.categorie.name }}</td>
                </tr>
                <tr>
                    <td>Année</td>
                    <td>{{ moto.year }}</td>
                </tr>
                <tr>
                    <td>Kilométrage : </td>
                    <td>{{ moto.kilometrage }}</td>
                </tr>
                <tr>
                    <td>Puissance din/fical</td>
                    <td>{{ moto.din }}/{{ moto.fisc }}</td>
                </tr>
            </table>
        </div>
    </div>
    <div class="text-primary pt-0 text-right" style="font-size: 3rem; font-weight: bold">{{ moto.formattedPrix }}€</div>
        <a href="#" class="btn btn-primary ">Contacter le vendeur</a>
    <div class="col-12 ">
        <div class="row">
            <div class="col-6 ">
                <a href="{{ path('moto_index') }}">back to liste</a>
            </div>
            <div class="col-6 text-center">
                <a href="{{ path('moto_edit', {'id': moto.id}) }}">edit</a>
            </div>
        </div>
    </div>
    {{ include('moto/_delete_form.html.twig') }}
{% endblock %}
